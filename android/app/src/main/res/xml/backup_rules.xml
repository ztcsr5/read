<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- 排除Hive数据库（包含书源和阅读进度，通过自有机制备份） -->
    <exclude domain="file" path="hive/" />
    <!-- 排除缓存 -->
    <exclude domain="file" path="cache/" />
    <!-- 排除临时文件 -->
    <exclude domain="file" path="tmp/" />
</full-backup-content>
